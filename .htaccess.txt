RewriteEngine On
RewriteBase /gasparmarcano-sitioweb/

# Redirigir la raíz al index.php dentro de public
RewriteRule ^$ public/index.php [L]

# Si el archivo no existe, redirigir a public
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/$1 [L]